{
  "name": "wanderingarrow",
  "version": "3.0.0",
  "main": "server.js",
  "scripts": {
    "build": "npx @11ty/eleventy",
    "start": "rm -rf dist && npx @11ty/eleventy --serve --port=3000"
  },
  "dependencies": {
    "@11ty/eleventy": "^1.0.2"
  },
  "engines": {
    "node": "18.x"
  },
  "repository": {
    "url": "https://github.com/patrickweaver/pw20/blob/main/package.json"
  }
}
